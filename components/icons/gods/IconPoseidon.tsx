import { IconBase } from "../IconBase";

export const IconPoseidon = ({ size = 42, className }) => {
  return (
    <IconBase
      iconKey="gods/poseidon"
      alt="Poseidon"
      size={size}
      className={className}
    />
  );
};
