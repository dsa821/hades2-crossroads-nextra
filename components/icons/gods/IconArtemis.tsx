import { IconBase } from "../IconBase";

export const IconArtemis = ({ size = 42, className }) => {
  return (
    <IconBase
      iconKey="gods/artemis"
      alt="Artemis"
      size={size}
      className={className}
    />
  );
};
