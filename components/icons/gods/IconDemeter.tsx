import { IconBase } from "../IconBase";

export const IconDemeter = ({ size = 42, className }) => {
  return (
    <IconBase
      iconKey="gods/demeter"
      alt="Demeter"
      size={size}
      className={className}
    />
  );
};
